# markdownlint-cli2 configuration.
config:
  default: true
  # Allow long lines (URLs, tables) — line length is not a useful gate here.
  MD013: false
  # Allow inline HTML (badges, details/summary).
  MD033: false
  # First line in file need not be a top-level heading.
  MD041: false
globs:
  - "**/*.md"
ignores:
  - "**/node_modules/**"
  - "**/vendor/**"
