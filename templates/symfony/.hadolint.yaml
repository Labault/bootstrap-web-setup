# Managed by bootstrap. hadolint (Dockerfile linter) config.
failure-threshold: warning

trustedRegistries:
  - docker.io
  - ghcr.io

# Silence specific rules here if needed, e.g.:
# ignored:
#   - DL3008
